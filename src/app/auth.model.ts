//import { bcrypt } from "bcryptjs";

export class Auth {



    /*constructor(){ 
        var salt = bcrypt.genSaltSync(10);
        var hash = bcrypt.hashSync("vito", salt);
        console.log(hash);
    }*/

}