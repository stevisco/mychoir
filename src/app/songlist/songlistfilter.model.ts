export class SongListFilter {

    public alpha: string[];
    public tags:string[];

    constructor(tags:string[],alpha:string[]){
        this.alpha = alpha;
        this.tags = tags;
    }

    
}

